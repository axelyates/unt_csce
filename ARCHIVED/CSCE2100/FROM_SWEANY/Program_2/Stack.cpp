#include "Stack.h"

    Stack::Stack()
    {
    }


    Stack::~Stack()
    {
    }

    void Stack::push(HashTable& ht) 
            // Hash Tables are "elements" of the stack
    {
    }

    HashTable& Stack::pop () // removes the top of the stack
    {
    }    

    HashTable& Stack::top ()    // returns a reference to the
                                // top of the stack.  Doesn't 
                                // really remove it.
    {
    }
